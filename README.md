

```python
# 
from enum import Enum

# 
class SimpleComputer:
    """
 

    # 
    class Register:
        def __init__(self, name, value=0):
            self.name = name
            self.value = value

    # 
    class Memory:
        def __init__(self, size=100):
            self.size = size
            self.data = [0] * size
