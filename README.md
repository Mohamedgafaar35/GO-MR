

```python
# 
from enum import Enum

# 
class SimpleComputer:
    """
 

    # 
    class Register:
        def __init__(self, name, value=0):
            self.name = name
            self.value = value
